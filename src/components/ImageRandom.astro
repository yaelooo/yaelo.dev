---
const num = Math.floor(Math.random() * 4);

interface Props {
  classes: string;
  alt: string;
  section: string;
}

const { classes, alt, section } = Astro.props;
---

<img src={`${section}/${num}.webp`} alt={alt} loading="lazy" class={classes} />
